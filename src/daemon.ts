import { NS } from '../types/index';
import * as h from 'helpers';
const intTrack = [[]];
export async function main(ns: NS) {
	while (true) {
		// Core daemon loop

		await ns.sleep(0);
	}
}

async function intervalProcessor(identifier: string, interval: number) {}
